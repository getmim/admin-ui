<?php
    $value = (string)$value;
    $attrs = [
        'type'        => 'hidden',
        'id'          => $id,
        'name'        => $field->name,
        'value'       => $value
    ];

    if(isset($rules->required) && $rules->required)
        $attrs['required'] = null;
    if(isset($rules->empty) && !$rules->empty)
        $attrs['required'] = null;
?>
<div class="form-group<?= ($error?' is-invalid':'') ?>">
    <?php if($show_label): ?>
        <label for="<?= $id ?>"><?= $field->label ?></label>
    <?php endif; ?>
    <div class="date daypicker">
        <input<?= to_attr($attrs) ?>>
        <div class="daypicker-control"></div>
    </div>
    <div class="invalid-feedback"><?= ($error?$error->text:'') ?></div>
</div>
